[project]
name = "amr-fed"
version = "0.1.0"
description = "Topology-aware federated learning for antimicrobial resistance KGs"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
